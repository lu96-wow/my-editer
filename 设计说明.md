# core-chinese 设计说明

> 用中文标识符版本（`core-chinese/`）讲清：每一层 API 的设计思路、最终 `编辑器` 的设计思路，
> 以及**每个参数**到底控制什么逻辑。名字全部用 `core-chinese` 里的叫法（例如 `位置` = `point`）。

---

## 0. 全局设计原则（先立规矩，再看 API）

1. **一切皆不可变数据，一切操作是纯函数**
   所有值（`位置`/`选区`/`缓冲`/`文档`/`窗口`/`屏幕`/`编辑器`）都是不可变值；
   操作形状是 `data -> lambda -> data`。没有隐藏状态、没有副作用、没有 `set!` 式的就地改写。
   于是「撤销」「多视图同步」「并发快照」都退化成「持有旧值」，不需要额外机制。

2. **目录 = 依赖层，依赖只许向下**
   `atom ← unit ← doc ← viewport ← platform`。同层可互相依赖但必须无环。
   从文件路径就能读出「我在哪层、我能 require 谁」。

3. **文档态 / 视图态 / 派生态 三分**
   - 文档态：文本 + 属性（可编辑、随编辑移动、进账本）。
   - 视图态：光标/选区、滚动、尺寸、模式（临时，不进账本）。
   - 派生态：外观（face）、语法高亮等 = 投影参数，**不进文档**，现算，不需要失效机制。

4. **唯一变更漏斗**
   文本 + 属性变更都打包成 `变更`，只经 `文档-施加-变更` 一处施加。
   顺序固定：文本 `内容-施加` → 只读守卫 → 属性跟随 → 显式属性批 → 版本戳 +1。

5. **一个编辑原语 + 正交策略参数**
   编辑只有 `编辑器-命令` / `编辑器-命令-批`；目标/上下文/守卫/反应/记账全是**参数**，
   不是「不同函数各自实现一遍」。其余入口都是它的薄封装。

6. **违约行为二分**
   有唯一合法解释 → 夹紧/归一（越界位置 `位置-夹紧`、零宽属性描述 = no-op）；
   没有合法解释 → 报错（反向区间、跨行属性、未知同步/模式……）。

7. **可达面是显式白名单**
   `api.rkt`（低层）+ `editor.rkt`（平台面）分开；带不变量的值只透出谓词、读口、具名构造器，
   不透 `struct` 构造器。内部机制（`state`/`write`/`反应`）不进白名单。

---

## 1. atom —— 原子层：不可再分的值 + 代数

设计思路：把「位置」「选区」「一次替换」「一条属性变更」这些**最小的语义单位**定死，
并让它们只依赖同层的其它原子。上层无论怎么组合，最终都落到这几张契约上。

### 1.1 `位置`（point）
- 唯一位置表示：`(struct 位置 (行 列))`，全部 0-based，`列` 是**字符索引**（不是显示列）。
- 设计思路：位置只回答「第几行第几列」，**不知道**行有多长；合法性由调用方夹紧。
  这样位置传递永远是一个值，不会出现「两个数字谁先谁后」的约定。

| API | 参数逻辑 |
|---|---|
| `位置 行 列` | 裸构造，不做校验 |
| `位置<?` / `位置=?` / `位置<=?` | 字典序比较，逐字符比较 |
| `坐标<? l1 c1 l2 c2` | 直接比 `(行,列)`，给「还没构造位置」的场景用，零分配 |
| `位置-夹紧 p 行-数量 行-长度` | `行-数量` ≥ 1；`行-长度` 可以是数，也可以是 `行号→长度` 的函数；越界夹到唯一合法域 |
| `位置-左/右/行首/末尾` | 纯值算子，不依赖文档；上下左右在 viewport 层（要窗口） |

### 1.2 `选区` / `选区集`（selection / selection-set）
- 选区 = `(struct 选区 (锚点 头部))`，空选区就是光标；`头部` 是「活动端」，`锚点` 是「不动端」。
- 选区集 = `(struct 选区集 (名称 选区列表 主导-索引))`：**多光标 = 一次同构操作的临时单位**。
- 设计思路：组有名字（身份）、已归一化的区间、一个**主导**（原来的单选区）。
  何时清理由上层决定——core 不做自动收敛。

| API | 参数逻辑 |
|---|---|
| `选区-范围 s` | 返回 `(values 起点 末尾)`，**已排序**（与锚点/头部方向无关） |
| `选区-附-头部 s 位置` | 动**头部**（锚点不动）：`(选区 (选区-锚点 s) 位置)` |
| `选区-附-锚点 s 位置` | 动**锚点**（头部不动）：`(选区 位置 (选区-头部 s))` |
| `选区-映射-头部/锚点/两端 s f` | 对端点施 `位置→位置`，是纯原子变换 |
| `选区-空?` | 锚点 = 头部 |
| `选区集-打开 名称 选区列表 主导-索引` | 构造并规范化；`主导-索引` 指向「真正的主」 |
| `选区集-规范化 g` | 排序/去重/合并重叠，仍按身份追踪主导 |
| `选区集-映射 g f` / `选区集-映射-编辑 g f` | 对整个集合施同构变换 |

### 1.3 `编辑-描述` / `属性-描述` / `变更`（edit-desc / attr-desc / change）
- `编辑-描述 起点 末尾 新-文本`：半开区间 `[起点,末尾)` 替换成 `新-文本`。这是**唯一跨层文本变更契约**。
- `属性-描述` = `(起点 末尾 键 操作 值)`，同一行、半开，`操作 ∈ {'set, 'remove}`（数据符号保留英文，构造器叫 `属性-设置`/`属性-移除`）；零宽 = no-op。
- `变更 = 文本集 ⊕ 属性集`：把两种变更打包成一个原子单位。
- 设计思路：变更值一旦造出，就自含「改什么、怎么改」，不依赖任何上下文。

| API | 参数逻辑 |
|---|---|
| `编辑描述-之后的-位置 d p` | 编辑前位置 `p` → 编辑后位置；落在被删区间则返回 `#f` |
| `编辑描述-映射位置 d` | 同上，柯里化版本 |
| `编辑列表-规范化 ds` | 排序、去重、合并相邻；保证 desc 两两不相交 |
| `编辑列表-跨度 ds` | 所有 desc 影响的行区间并集（`#f` = 空） |
| `编辑列表->变更 ds` / `属性集->变更 as` | 打包成 `变更` |
| `属性-设置 起点 末尾 键 值` / `属性-移除 起点 末尾 键` | 造属性描述；同一行是硬约束 |
| `属性集->变更 as` | 只含属性的 `变更` |

### 1.4 `内容` / `宽度` / `事件`（content / width / event）
- `内容` = 行向量文本存储；`行列表->字符串` / `字符串->行列表`（`lines.rkt`）固定「字符串 ↔ 行序列」的唯一约定。
- `宽度` 只做几何换算（宽字符 = 2 列，组合字符 = 0 列），**不加空格**——终端自己会画宽字符。
- `事件` 是类型化输入：`修饰键(控制 Alt 平移 Meta)`、`文本事件`、`键事件`、`鼠标按下事件`、`鼠标滚轮事件`、`尺寸变更事件`、`退出事件`。

| API | 参数逻辑 |
|---|---|
| `字符-显示-宽度 c` / `字符串-显示-宽度 s` | 宽字符 2、组合/格式字符 0、其余 1；Ambiguous 按 1 |
| `索引->显示列 s i` / `显示列->索引 s 列` | 字符索引 ↔ 显示列；宽字符右半格命中同一字符 |
| `吸附-显示列-前向 s 列` | 显示列吸附到字符起点；落在宽字符右半格则后移 |
| `修饰键 控制 Alt 平移 Meta` | 四个布尔位；`修饰键-平移` 是 Shift |

---

## 2. unit —— 单元层：由原子组合出的「单维结构」

设计思路：每种「一维标注/索引」= 一种结构 + 它自己的 `apply-edit`（跟随文本移动）。

### 2.1 `属性集`（attrs）
- `行内跨度 rspan = (起点 末尾 值)`：一行的属性区间。
- `属性集 = (struct 属性集 (屏行列表))`，本质是「行 → 行内跨度列表」。
- 键是通用的 `键→hash`，core 只解释保留键 `只读`；其余对 core 不透明。
- 核心操作：`属性集-施加-编辑`（跟随文本移动）、`属性集-施加-属性`（施加显式属性描述）、
  `属性集-替换-描述集`（把某键在一段行范围内整体替换）。
- **为什么属性要和文本并行存放**：属性可以随编辑移动、可精确撤销；如果塞进文本就成了转义字符，全乱套。

### 2.2 `屏幕`（screen）
- 一帧 = `(struct 屏幕 (高度 宽度 屏行-片段集 光标列表 选区列表))`。
- `片段 run = (列 文本 外观)`；`光标 cursor`、`区域 region`（选中段）、`窗格 pane`（用于多窗格合成）。
- 有两条独立通道：**文档文本**（`屏行-片段集`，来自缓冲 + 外观-提供者）与
  **视图覆盖层**（`光标列表`/`选区列表`，来自窗口）。两者不混。
- `外观` 是**不透明语义值**，core 不给颜色；前端把语义映射成样式。

| API | 参数逻辑 |
|---|---|
| `屏幕-行 s i` | 第 i 行的片段列表；越界报错 |
| `屏幕->屏行列表 s` | 所有行 |
| `屏幕-屏行->字符串 s i` | 第 i 行纯文本；片段间补空格、末尾裁掉（给测试/文本后端） |
| `屏幕-合成 窗格列表 高度 宽度` | 把多个 `窗格` 拼成一帧（窗口管理在 L6） |
| `屏幕-损伤 旧 新` | 逐行 diff，给增量重绘用 |

### 2.3 `历史`（history）
- 一步 = `(struct 步骤 (重放-反向 撤销 前-位置))`，自含正反两向。
- 账本 = `(struct 历史 (撤销 重做))`；合并规则是**结构判定**（纯文本单字符的连续段），无时钟无状态。
- 逆必须由**编辑前**的缓冲导出；用编辑后的缓冲求逆会静默写坏历史。
- `重放-反向` 逆序存储是为了合并 O(1)（正序取回由 `步骤-重放` 负责）。

| API | 参数逻辑 |
|---|---|
| `历史-记录 h 重放 撤销 前-位置` | 记一步；能合并就并进栈顶，否则压栈；任何记录清空重做栈 |
| `历史-弹出-撤销 h` / `历史-弹出-重做 h` | 弹一步并交换栈；空栈返回 `#f`（不报错） |
| `历史-撤销-深度 h` / `历史-重做-深度 h` | 步数 |
| 合并触发条件 | 打字：单字符非换行插入且 `d.起点 = after(pl)`；退格：单字符删除且 `d.末尾 = pl.起点`；前删：同上且 `d.起点 = pl.起点` |

---

## 3. doc —— 文档层：把单元装配成一个可编辑值

### 3.1 `缓冲`（buffer）
- `(struct 缓冲 (内容 版本戳))`：纯文本 + 版本戳。缓冲**没有光标**。
- `版本戳` 只有文本变更才 +1（一次变更里多少条文本 desc 都只 +1）。

### 3.2 `文档`（document）
- `(struct 文档 (缓冲 属性集 属性-版本戳))`：文本与标注分离，视图持有文档。
- `文档-施加-变更` 是**唯一传播点**：文本先走 `内容-施加` 产出生效 desc → 只读守卫 →
  属性 `属性集-施加-编辑` 跟随 → 施加变更的属性部分 → 各版本戳 +1 → 一条 `变更-结果`。
- `变更-结果` 携带撤销材料：
  - `已施加-文本集` / `文本-逆集`（与施加顺序平行，逐条逆序施加）；
  - `已施加-属性集` / `属性-逆集`（逐条 `属性集-描述-逆`）；
  - `已抹除-恢复集`：文本编辑抹掉的属性在**原坐标**补回——这是「撤销删除不丢标注」的关键。

### 3.3 `编辑列表->变更`（batch）
- 把一串 `编辑-描述` 变成一条 `变更`，一次原子施加、一步撤销。
- 多光标编辑就靠它：每个选区算一条 desc → 规范化成不相交 → 打包一次施加。

---

## 4. viewport —— 视口层：把文档投影成画面

### 4.1 `窗口`（window）
- `(struct 窗口 (文档 选区集 模式 顶行 左列 顶段 高度 宽度 行号?))`
- `模式 ∈ {裁剪, 折行}`；`顶行`/`左列`/`顶段` 是滚动位置；`行号?` 是视图装饰开关。
- 设计思路：窗口 = 文档引用 + 光标/选区（视图态）+ 滚动/尺寸；它**不懂**怎么渲染，只持状态。

### 4.2 `渲染`（render）
- `渲染-行 缓冲 行 外观-提供者 → 字形集`。派生外观在这里**注入**，不存进文档。
- 外观-提供者 : `缓冲 × 行 → 片段集`；未覆盖段外观 = `#f`。

### 4.3 `布局`（layout）
- 把窗口变成 `虚拟行`（visual row）序列：`虚拟行 = (struct 虚拟行 (行 起点-列 末尾-列))`。
- `裁剪`：一行缓冲 = 一条虚拟行，列范围 = `[左列, 左列+宽度)`。
- `折行`：一行缓冲 = 若干段，每段宽 ≤ 宽度，**宽字符绝不切半**。
- 布局还负责：光标/鼠标映射、`确保`（把某位置滚进可见区）、视觉上下移动、行号栏宽度。
- **行号栏是派生量**：`窗口-边栏-宽度` = 视口行号上界位数 + 1，正文宽 = 总宽 − 栏宽；
  layout 内部一律用正文宽，`project` 再把栏 run 前置。

### 4.4 `投影`（project）
- `窗口 → 屏幕`：拼行号栏 + 光标/选区 overlay。
- 行号只标在缓冲行的**首段**（折行续段留空）；鼠标点 gutter 落到行首。

### 4.5 `重基准`（rebase）
- `窗口 × 编辑-描述 → 窗口`，两种模式：
  - `自由`：把光标映射过这次编辑，不滚屏。
  - `跟随`：把光标映射 + 复制视口（可能跟随另一视口）。
  - `主导`：推进到插入后 + `确保` 定稿。
- 契约：`自由`/`跟随` **只在同一文档的视口之间**发生。

### 4.6 `镜像`（mirror）
- `窗口 × 窗口 → 窗口`：跨文档的视口映射（链接 link 用）。
- 只改成员的**视口**，不改它的文档，也不动它的选区；投影 = 行固定行号（不够夹最近）+ 列按比例。

---

## 5. platform —— 平台层：多文档 × 多视口 + 三种面

### 5.1 `state`（数据，内部）
- `编辑器 = (struct 编辑器 (文档列表 视口列表 焦点 下一个-文档 下一个-视口))`。
- `文档-条目 = (struct 文档-条目 (标识 名称 文档 历史 记录?))`。
- `视口 = (struct 视口 (标识 窗口 同步 链接))`；`同步 ∈ {自由, 跟随}`，`链接` 是符号名或 `#f`。
- `变更-报告 = (struct 变更-报告 (文本集 属性集))`。
- 标识（`文档标识`/`视口标识`）是**稳定句柄**（单调递增、关闭后不复用），不是 list 下标。
- 不变量：任一视口的 `窗口.文档` 必是某个 `文档-条目` 的文档。

### 5.2 `write`（无策略写原语，内部）
- 能破坏不变量的动作全藏在这里，不进入口：
  `编辑器-交换-文档`（换文档引用，entries + 同文档视口一起换）、`编辑器-施加-变更`（唯一漏斗）、
  `编辑器-安装-视口`（换一个视口的窗口，夹紧视口）、`编辑器-安装-视口-链接`、
  `编辑器-设置-视口-同步`、`编辑器-设置-视口-文档`、`编辑器-安装-历史` 等。
- `编辑器-交换-文档` **不动光标、不滚屏、不确保**——显示决策留给 `反应`。

### 5.3 `neutral`（中性面，公开）
- 只读投影 + 构造 + 解析 + 属性读 + 投影，**不含写原语、不含显示决策**。
- 构造器：`编辑器-打开`、`编辑器-打开-文档`、`编辑器-添加-视口`、`编辑器-关闭-*`、`编辑器-焦点-*`。
- 读口分两级：按焦点糖的 `编辑器-*`，按显式 `视口标识` 的 `编辑器-视口-*`，按 `文档标识` 的 `编辑器-文档-*`。
- 投影：`编辑器->屏幕`、`编辑器-视口->屏幕`、`编辑器-位置->屏幕`、`编辑器-屏幕->位置`。

### 5.4 `反应`（reaction，内部）：唯一的显示语义层
- 内容面只管「换文档引用」，光标一个都不碰；本层决定视图怎么反应，三选一：
  - `none`：只把光标/视口**夹回合法域**（文本可能变小），不映射。
  - `map`：每个同文档视口**各自**把光标映射过这次编辑，不滚屏。
  - `主导`：指定视口推进到插入后 + `确保`；同**链接**成员按镜像（可跨文档）；同文档其余自由映射/跟随复制。
- 另有 `编辑器-主导-窗口`：用户导航用——把一个视口窗口定稿，同链接镜像，同文档跟随镜像。
- `编辑器-对齐-链接`：把链接组对齐到参考成员（`从` → 焦点 → 组内第一个）。

### 5.5 `program`（程序面，公开）
- 编辑原语：`编辑器-命令` / `编辑器-命令-批`。其余（`编辑器-文档-编辑-在`、`编辑器-编辑`、属性写）都是薄封装。
- 显式视口命令：全部按 `视口标识` 定位，**只动指定视口，不镜像、不抢焦点、不确保**。
- 编辑动作（op 值）：`编辑-插入`、`编辑-插入-字符`、`编辑-换行`、`编辑-退格`、`编辑-删除`、`编辑-拼接`。
  op 形状统一是 `编辑器 文档标识 选区 → 编辑-描述/#f`，只**算**不施加。

### 5.6 `command`（用户面，公开）
- 用户编辑 = `编辑器-命令` + `#:反应 '主导` + `#:记录? '默认`。
- 导航 = `编辑器-视口-安装-窗口`（裸写）+ `窗口-*` + `编辑器-视口-跟随` 的组合（即 `编辑器-视口-移动`）。
- 撤销/重做：弹出账本一步 → 逐条施加 → 撤销后主导光标回到该步 `前-位置` 并 `确保`。
- 焦点糖：`编辑器-编辑` / `编辑器-撤销` / `编辑器-左` / …… 都转发到焦点视口。

---

## 6. 最终 `编辑器` 的设计思路

一句话：**编辑器 = 文档列表 × 视口列表 × 焦点 + 发号器**，配合「一个变更漏斗 + 一个编辑原语 + 一层显示语义」。

```
编辑器
 ├─ 文档列表 : [文档-条目]    每个 = 标识 · 名称 · 文档(缓冲⊕属性集) · 历史 · 记录?
 ├─ 视口列表 : [视口]         每个 = 标识 · 窗口(文档⊕选区集⊕滚动/尺寸) · 同步 · 链接
 └─ 焦点     : 视口标识 | #f
```

- **文档与视图生命周期分开**：一个文档可开多个视口（分屏），各有独立光标，共享同一份文本/账本。
- **内容流**：`op` → `编辑-描述` → `变更` → `文档-施加-变更` → `变更-结果` → `编辑器-交换-文档`。
- **渲染流**：`缓冲 → 渲染 → 片段 → 窗口->屏幕 → 屏幕`（后端画）。
- **显示决策**：内容换完后，`反应` 层按 `none`/`map`/`主导` 调整光标/视口。
- **多光标**：一组选区 → 每个选区一条 desc → 合并/去重 → 一次施加、一步撤销。
- **链接（link）**：同链接名的视口可跨文档同步**视口**（不改文档/选区）；同文档的 `自由`/`跟随` 负责光标。
- **版本戳**：文本版本 `文档-文本-版本戳`、标注版本 `文档-属性-版本戳`，各自独立 +1，`内容-相等?` / `属性集-相等?` 判断是否同一。

---

## 7. 参数逻辑速查（重点）

### 7.1 `编辑器-命令`（编辑原语）—— 七个正交参数

```racket
(编辑器-命令 编辑器 操作
  #:属性集    属性计划      ; 默认 #f
  #:视口      目标视口标识  ; 默认焦点视口
  #:选区      编辑上下文    ; 默认该视口当前选区集
  #:受信?      布尔          ; 默认 #f = 守只读
  #:反应      策略          ; 默认 'none
  #:记录?      记账策略      ; 默认 '默认
  #:编辑前位置 位置          ; 默认该视口主选区头部
)
```

| 参数 | 取值 | 逻辑 |
|---|---|---|
| `操作` | `(编辑器 文档标识 选区 → 编辑-描述/#f)` | 只**算**变更，不施加；每个选区算一条，重叠的合并成包络重算 |
| `#:属性集` | `#f` 或 `(编辑器 文档标识 生效描述集 → 属性描述集)` | 属性计划在文本 desc **夹紧后**求值，坐标为「文本生效之后」；与文本合成同一条 `变更` |
| `#:视口` | 视口标识 | 目标视口；决定编辑上下文、反应基准、记账 |
| `#:选区` | `#f` 或选区列表 | 编辑上下文；`#f` = 用该视口当前选区集 |
| `#:受信?` | `#f`(守) / `#t`(绕) | 是否跳过 `只读` 守卫；绕行只有这一个显式开关，没有全局 inhibit |
| `#:反应` | `'none` / `'map` / `'主导` | 见下 |
| `#:记录?` | `'默认` / `#t` / `#f` | `'默认` 跟随文档历史策略；`#t` 强制记；`#f` 不记 |
| `#:编辑前位置` | 位置 | 撤销回落的编辑前光标；默认主选区头部；批量默认最左施加点 |

反应三态：

| 值 | 光标 | 滚动 | 跨视口 |
|---|---|---|---|
| `'none` | 字面不动，只夹紧回合法域 | 不动 | 全都不动 |
| `'map` | 每个同文档视口各自**自由映射** | 不滚屏 | 同文档；跨文档不动 |
| `'主导` | 目标视口推进到插入后 | **确保**（滚进可见） | 同链接镜像视口；同文档跟随复制、其余自由映射 |

返回：`(values 编辑器 (or/c #f 变更-报告))`；`变更-报告` 带生效文本/属性（施加顺序）。

### 7.2 `编辑器-命令-批`
同 `编辑器-命令`，但直接吃现成的 `变更`（不做选区→op 那步）；
参数少了 `#:属性集`/`#:选区`，多一个 `#:前-位置` 的含义同 `#:编辑前位置`。

### 7.3 构造 / 生命周期

| API | 参数 | 逻辑 |
|---|---|---|
| `编辑器-打开 文本 [高度 24] [宽度 80]` | `#:名称 "*scratch*"` | 文档名 |
| | `#:历史? #t` | 该文档是否记账本（策略位） |
| | `#:行号? #f` | 首个视口是否显示行号 |
| `编辑器-打开-文档 ed 文本 [高度] [宽度]` | `#:名称`、`#:焦点? #f`、`#:历史?`、`#:行号?` | 新增文档 + 首视口；`#:焦点?` 默认**不抢**焦点；返回 `(values ed 文档标识)` |
| `编辑器-添加-视口 ed 文档标识 [高度] [宽度] [位置]` | `#:同步 '自由` | 新视口的同步策略：`'自由` / `'跟随` |
| | `#:焦点? #f` | 是否把焦点交给新视口（默认不抢） |
| | `#:链接 #f` | 加入的视口同步链接名（符号 / `#f`） |
| | `#:行号? #f` | 是否显示行号栏 |

### 7.4 视口状态设置（只动指定视口）

| API | 参数逻辑 |
|---|---|
| `编辑器-视口-设置-模式 模式` | `裁剪` 或 `折行`；非法报错 |
| `编辑器-视口-设置-行号 开?` | 行号栏开关（视图装饰） |
| `编辑器-视口-设置-顶行 n` / `设置-左列 n` / `设置-顶段 n` | 裁剪用左列，折行用顶段；值保留、互不干扰 |
| `编辑器-视口-设置-尺寸 高度 宽度` | 帧尺寸 |
| `编辑器-视口-设置-同步 同步` | `'自由`/`'跟随`（不改文档） |
| `编辑器-视口-设置-文档 文档标识` | 换属主文档（窗口夹紧） |

### 7.5 选区 / 选区集

| API | 参数逻辑 |
|---|---|
| `编辑器-视口-设置-选区列表 sels [主选区-索引 0]` | 安装整个列表；`主选区-索引` 指定谁是主 |
| `编辑器-视口-添加-选区列表 sels #:主选区? #f` | 追加；`#:主选区?` 决定新选区是否夺主 |
| `编辑器-视口-安装-选区集 g` | 安装命名选区集；`清除` 收敛为单个主导 |
| `编辑器-视口-映射-选区列表 f` | 对每个选区施 `f`，主选区保持 |
| `编辑器-视口-映射-主选区 f` | 只动主选区 |
| `编辑器-视口-映射-位置列表 f` | 对每个头部施 `位置→位置`，坍缩成光标 |

### 7.6 链接（link）

| API | 参数 | 逻辑 |
|---|---|---|
| `编辑器-视口-设置-链接 链接` | `#:对齐? #t` | 设链默认**立即对齐**；`#f` 只设成员 |
| | `#:从 #f` | 对齐参考成员 = `从`（若在组内）→ 焦点视口（若在组内）→ 组内第一个 |
| `编辑器-链接-视口列表 名称 vids` | `#:对齐?`/`#:从` | 组替换语义：列表外的旧成员退出；列表内不存在的标识报错 |
| `编辑器-视口-解除链接` | — | 设 `链接 = #f` |

### 7.7 属性写 / 批量写

| API | 参数逻辑 |
|---|---|
| `编辑器-文档-施加-属性集 as #:记录? '默认` | 只写属性；走变更命令，光标自然不动 |
| `编辑器-文档-安装-属性 键 行 c0 c1 值 #:记录?` | 单段 `设置`；坐标 = 行 + 半开列区间（属性天生同行） |
| `编辑器-文档-移除-属性 键 行 c0 c1 #:记录?` | 单段 `移除` |
| `编辑器-文档-替换-属性 键 跨度集 #:行列表 l0 l1 #:记录?` | 把某键在 `[l0,l1]` 行整体替换（先清后设）；`跨度集 = (行 c0 c1 值)` |
| `编辑器-文档-施加-编辑列表 描述集 #:记录?` | 批量文本施加（程序面，显式文档标识） |

### 7.8 历史策略

| API | 参数逻辑 |
|---|---|
| `编辑器-设置-历史-启用 开?` / `编辑器-视口-设置-历史-启用 开?` | 改该文档默认是否记账 |
| `编辑器-文档-清空-历史 [文档标识]` | 清栈（不隐式清）；默认焦点文档 |
| `编辑器-文档-可撤销?` / `可重做?` / `撤销-深度` / `重做-深度` | 账本查询 |

### 7.9 投影 / 坐标

| API | 参数逻辑 |
|---|---|
| `编辑器->屏幕 ed [外观-提供者 空外观提供者]` | 外观-提供者 : `编辑器 文档标识 行 → 片段集`（内部适配成缓冲级 `缓冲 × 行 → 片段集`） |
| `编辑器-位置->屏幕 ed` | 把**当前光标**投影成 `(values 屏行 屏幕列)`；不可见 → `(values #f #f)`；列已含栏宽偏移（显式视口版：`编辑器-视口-位置->屏幕 ed 视口标识`） |
| `编辑器-屏幕->位置 ed 屏行 屏幕列` | 反投影；点 gutter 落到行首；越界 → `(values #f #f)`（显式视口版：`编辑器-视口-屏幕->位置`） |
| `编辑器-滚动 增量` | 导航后 `确保` + 跟随镜像 |

---

## 8. 一句话总结每层「为什么长这样」

| 层 | 一句话 |
|---|---|
| atom | 把最小语义单位（位置/选区/替换/属性变更）定死，位置只是一个值 |
| unit | 每种标注 = 一种结构 + 自己的跟随规则（属性集跟随文本、历史记账本） |
| doc | 文本与属性分离，但共用一个变更漏斗，保证坐标一致 + 可精确撤销 |
| viewport | 文档是数据，视口是「怎么看」；投影参数（外观）现算，不存、不过期 |
| platform | 多文档多视口 + 三种面（中性/程序/用户）；策略是参数，不是函数身份 |
